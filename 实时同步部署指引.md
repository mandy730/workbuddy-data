# 🌸 妈妈工作台 · 实时同步部署指引（手把手版）

目标：把工作台发布到网上，手机和电脑打开**同一个网址**，数据**实时同步**。
全程在网页上点几下，不用装软件、不用输命令。预计 5–10 分钟。

---

## 第 1 步：在 GitHub 新建仓库（存代码的地方）

1. 打开 https://github.com 并登录
2. 点右上角 **「+」→「New repository」（新建仓库）**
3. 填写：
   - **Repository name（仓库名）**：`mama-hub`
   - **可见性**：选 **Public（公开）**
   - **不要**勾选下面的 "Add a README file" / "Add .gitignore" / "Choose a license"（保持空仓库，避免冲突）
4. 点绿色按钮 **「Create repository」（创建仓库）**

## 第 2 步：把工作台文件传上去

1. 创建成功后，页面会提示 "create a new repository on the command line" 之类的教程——**不用管它**。
2. 找这一句链接：**「uploading an existing file」**（或在空白处点 **「Add file」→「Upload files」**）
3. 打开你电脑上的文件夹：
   `访达 → 桌面 → workbuddy工作台 → mama-hub`
4. 把里面**除 `hub-data.json` 以外的所有文件**一次性拖进 GitHub 的虚线框（可多选拖拽）：
   - `app.js` `index.html` `styles.css` `manifest.json` `sw.js` `icon.svg`
   - `sync-server.js` `package.json` `render.yaml` `README.md` `实时同步部署指引.md`
5. 拉到页面最下面，点绿色按钮 **「Commit changes」**（直接提交到 main 分支即可）

> 等几秒，文件出现在仓库里就成功了 ✅

## 第 3 步：在 Render 一键部署（出网址 + 开同步）

1. 打开 https://render.com 并登录
2. 左上角点 **「New」→「Blueprint」**
3. 第一次会要求连接 GitHub：点 **「Connect」/「Authorize」**，跳到 GitHub 网页后点 **「Authorize Render」** 授权
4. 授权回来后，在仓库列表里选 **`mama-hub`** → 点 **「Connect」**
5. Render 会自动读取 `render.yaml`，显示要创建的服务 → 点 **「Apply」/「Create New Resources」**
6. 等待部署（日志会滚动，约 1–3 分钟）。状态变成 **「Live」** 后，点服务名就能看到你的网址，类似：
   `https://mama-hub.onrender.com`

## 第 4 步：开启手机⇄电脑实时同步

1. 用**电脑浏览器**打开第 3 步得到的网址（Render 那个）
2. 点右上角 **⚙（设置）** → **「数据与手机同步」**
3. 点按钮 **「📡 使用当前网址作为同步地址」**（自动填好）
4. 勾选 **「开启自动同步」**
5. 点 **「保存设置」**

完成！现在：
- 用**手机浏览器**也打开这个 Render 网址（和电脑同一个）
- 手机上同样点 ⚙ → 用当前网址 → 勾选自动同步 → 保存
- 之后任意一端勾选/新增/修改，另一端**几秒内自动出现**（已实测）

---

## 💡 小贴士

- **以后就用 Render 这个网址当主入口**：之前 CloudStudio 的预览链接是「静态版、没同步」，建议以 Render 网址为准。
- **加到主屏幕当 App**：手机浏览器打开网址 → Safari「分享 → 添加到主屏幕」/ 安卓「添加到桌面」。
- **免费额度说明**：Render 免费版长时间不用会「睡觉」，第一次打开可能慢几秒（唤醒），之后就正常，不影响同步。
- **更安全（可选）**：在 Render 控制台给服务加环境变量 `SYNC_KEY=你随便设的一串`，然后在 ⚙ 设置的「访问密钥」里填**同样**的那串，别人就改不了你的数据。
- **不想部署也能跨设备**：⚙ 里的「💾 导出备份 / 📂 导入备份」可手动在两台设备间搬数据。

## 🆘 卡住了？
- 报错多为「仓库名被占用」→ 换一个名字（如 `mama-hub-2026`），记得 `render.yaml` 不用改，部署时选对仓库即可。
- 网址打不开 → 等 1 分钟再刷新；看 Render 里服务是不是 "Build failed"，把红色日志发我。
